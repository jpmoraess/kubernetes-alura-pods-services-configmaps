apiVersion: v1
kind: Service
metadata:
  name: svc-pod-2
spec:
  type: ClusterIP
  selector:
    app: segundo-pod
  ports:
    - port: 9000
      targetPort: 80




# primeira versão do arquivo
# apiVersion: v1
#kind: Service
#metadata:
#  name: svc-pod-2
#spec:
#  type: ClusterIP
 # selector:
 #   app: segundo-pod
 # ports:
 #   - port: 80     